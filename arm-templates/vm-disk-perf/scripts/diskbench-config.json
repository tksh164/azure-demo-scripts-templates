{
    "webhook": {
        "url": "https://example.com/webhook",
        "method": "POST",
        "bodyForStart": {
            "message": "{{datetime}} - Benchmark started."
        },
        "bodyForEnd": {
            "message": "{{datetime}} - Benchmark completed."
        }
    }
}
